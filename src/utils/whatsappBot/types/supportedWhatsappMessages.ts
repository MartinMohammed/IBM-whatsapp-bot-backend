/** Represents the message types that are currently supported by this application. */
export enum SupportedWhatsappMessages {
  TEXT = "text",
}

/** Represents a supported WhatsApp message type. */
export type SupportedWhatsappMessageType = SupportedWhatsappMessages.TEXT;
