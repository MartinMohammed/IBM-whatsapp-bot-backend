// Verification of '/webhook'
export enum MetaVerificationFields {
  /**
   * The mode of the hub.
   */
  HUB_MODE = "hub.mode",
  /**
   * The challenge sent by the hub for verification.
   */
  HUB_CHALLENGE = "hub.challenge",
  /**
   * The verify token used for verification.
   */
  HUB_VERIFY_TOKEN = "hub.verify_token",
}
