import { onInlineQueryHandler } from "../onInlineQueryHandler";
import * as AuthorizeSenderModule from "../../../util/authorizeSender";
import TelegramBot from "node-telegram-bot-api";
import Constants from "../../../../Constants";

describe("Given is a demo inline query sent to the onInlineQuery telegram listener:", () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  it.todo("");

  afterAll(() => {
    jest.restoreAllMocks();
  });
});
