/** Represents the message types that are currently supported by this application. */
export enum SupportedTelegramMessages {
  TEXT = "text",
}

/** Represents a supported Telegram message type. */
export type SupportedTelegramMessageType = SupportedTelegramMessages.TEXT;
