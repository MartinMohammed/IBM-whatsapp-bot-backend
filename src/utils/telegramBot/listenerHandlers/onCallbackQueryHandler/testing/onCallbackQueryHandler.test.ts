import { onCallBackQueryHandler } from "../onCallbackQueryHandler";
import * as AuthorizeSenderModule from "../../../util/authorizeSender";
import TelegramBot from "node-telegram-bot-api";
import Constants from "../../../../Constants";

describe("Given is a demo callback query sent to the onCallBackQuery telegram listener:", () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  it.todo("");

  afterAll(() => {
    jest.restoreAllMocks();
  });
});
